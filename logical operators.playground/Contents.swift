
var x = 5 , y = 10
var z :Bool = x > y  //Bool

x > y  //bigger than
x < y  //less than
x >= y //bigger than or equal
x <= y //less than or equal
x == y //equal
x != y //not equal

var studentMark = 60
var isPass : Bool = studentMark >= 50
print(isPass)
print(!isPass) // not

// not -> ! ,  and ->  &&    , or -> ||
var isLastYear = true
var isGraduated = isLastYear && isPass

var a = true, b = false, c = true

print (!a || (c && b))

